import RegisterScreen from "@/components/registerComponent/register.component";

const RegisterPage = () => {
  return <RegisterScreen />;
};
export default RegisterPage;
export const unstable_settings = {
  initialRouteName: "registerScreen",
};
